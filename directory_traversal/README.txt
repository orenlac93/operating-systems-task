
how to compile:
	type (make)
	the makefile will create executable file (dir_traversal)

how to run:
	run (dir_traversal) with 1 parameter(folder_path)
	or without parameters (operate on the current folder)
	by typing: ./dir_traversal <path> 
	   

example:
	./dir_traversal dir

